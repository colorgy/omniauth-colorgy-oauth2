require 'spec_helper'

describe Omniauth::Colorgy::Oauth2 do
  it 'has a version number' do
    expect(Omniauth::Colorgy::Oauth2::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
